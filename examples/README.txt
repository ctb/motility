These examples use the Python module, because frankly C++ is an ugly
language.  So... you'll have to compile the Python module & then
modify the 'sys.path.append' at the top of each example file accordingly.

--titus
